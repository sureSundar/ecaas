<h1>New product</h1>

<%= render 'form' %>

<!-- %= link_to 'Back', products_path % -->
<%= link_to 'Back', account_store_products_path(store_id: @product.store.id,account_id: @product.store.account.id) %>
